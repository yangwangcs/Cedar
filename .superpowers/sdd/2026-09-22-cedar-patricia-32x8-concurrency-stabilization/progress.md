# SDD ledger — plan: docs/superpowers/plans/2026-09-22-cedar-patricia-32x8-concurrency-stabilization.md

Execution method: Native (`superpowers:executing-plans`).

Pre-flight: Task 1 produces the fixed 32-by-8 representation consumed by Tasks 2-8; commit identity and focused tests are the contract.
Pre-flight: Task 2 produces 128-byte `SegmentEdge` accessors consumed by Task 3 publication code and Task 5 memory/performance measurement.
Pre-flight: Task 3 produces diagnostic retry hooks consumed by Task 5 untimed diagnostics; hooks remain disabled in timed instances.
Pre-flight: Task 4 produces the runner/summarizer consumed by Task 5; production sample counts and gates match the approved spec.
Pre-flight: Task 5 selects the final source consumed by Tasks 6-8; no source change after the binding matrix without rerunning Tasks 5-7.
Pre-flight: Task 6 supplies freeze/Flush characterization consumed as evidence by Task 8.
Pre-flight: Task 7 supplies identical-source old/new fixtures and compatibility evidence consumed by Task 8.

Ruling: The user's blanket approval applies to all remaining design/plan execution gates in this thread; execute continuously without further stage confirmation.

Task 1: complete (commits d59d8e7..ee9a887; RED: missing `kByteSegmentCount`; GREEN: 44/44 focused C++ tests, both Python models and both negative controls; production files equal `48bba50`).
Task 2: complete (commit `520a5ce`; RED: missing `SegmentEdgeStrideForTesting`; GREEN: 45/45 focused tests; layout 128-byte stride, deterministic Branch >=4096 bytes and child blocks 72 bytes).
Task 2: Ruling: replaced the obsolete `block_bytes > branch_bytes/4` diagnostic with exact `block_bytes == 72`; cache-line padding intentionally changes Branch bytes but not immutable child-block allocation.
Task 3: Ruling: after a successful local retry, retraverse from the acquire root and monotonically update boundary caches on the current path. A concurrently published ancestor can preserve the old subtree while making the original traversal path incomplete; structural compatibility alone does not update the new ancestor's min/max cache.
Task 5: Ruling: the approved isolated A/B retention gates fail twice under substantial host scheduling noise (remote desktop and WindowServer CPU use); retain both raw A/B matrices, and treat the five-seed full matrix as diagnostic only. Neither noisy medians nor cross-seed averaging can authorize retention. Cost if wrong: a beneficial sub-change may be reverted, but correctness and the fixed 32x8 baseline remain intact.
Task 5: Ruling: the final matrix fails 8-writer ratio for three seeds, all five CV gates, and every 1/4-writer gate; revert both approved sub-changes and measure the restored baseline. Do not freeze or start persistence validation until the binding performance gates pass. Cost if wrong: lifecycle proof is deferred even though persistence may be sound.
Task 5: Ruling: add an untimed direct-index diagnostic executable outside the embedded engine to count the existing TestHooks in clean baseline/isolation/final libraries. The committed benchmark lacks a retry/restart observer at the MemTableRepFactory boundary; altering it would break the identical-source timing contract. The direct-index workload reproduces the normalized key and shuffle but is explanatory, never timed evidence. Cost if wrong: its contention counts may not exactly match the MemTableRep wrapper, so the Release matrix alone decides performance.
Task 5: candidate commits `c808202` and `520a5ce` reverted in `235340a` and `34b5080`; production index files now exactly match restored baseline `ee9a887`. Both A/B rounds failed retention. Candidate five-seed matrix: 400/400 valid rows, 0 errors, expected hash `454339457082336225`, binding gates failed. Reverted baseline five-seed matrix: 400/400 valid rows, 0 errors, same hash; one 8-writer ratio and all five CV gates failed. Task 5 remains incomplete; Tasks 6-8 have a performance prerequisite and have not started.
Task 5: Ruling: an OS-policy probe cannot satisfy the CV gate on the current host. With `taskpolicy -a`, 20 alternating seed-20260922 processes measured baseline Patricia median 23.017 ms, ratio 0.7748x SkipList, but CV 13.44%; SkipList CV was 43.79%. The host has four performance plus four efficiency cores, substantial competing CPU load, and the unchanged benchmark times writer creation and joining. Negative nice priority and passwordless elevation are unavailable. Preserve the raw resource counters and require a low-contention rerun rather than changing timing semantics or controlling unrelated processes. Cost if wrong: Task 5 remains open even if a different permitted scheduler policy could have produced binding evidence.
Task 5: Ruling: the highest unprivileged macOS latency and throughput policy (`taskpolicy -a -l 1 -t 1`, mapping to Tier 0) still fails: 20-process Patricia CV 8.99%, SkipList CV 17.51%, despite Patricia retired-instruction CV 1.19% and median ratio 0.7764x. All source-preserving OS policy options are exhausted. Cost if wrong: a privileged priority or externally isolated host could pass, but neither is available inside the approved worktree scope.
Task 5: blocked audit: third consecutive goal turn with the same external scheduling condition. Tier 0 revalidation at seed 20260922 measured Patricia median 27.592 ms, CV 14.54%, ratio 0.9258x SkipList; SkipList CV 12.29%. Current load remained 5.48 / 4.54 / 4.41 and passwordless elevation remained unavailable. Exact samples are committed as `scheduler-blocked-revalidation.json`. No approved source change or safe in-scope OS control remains.
Goal resumed: external load dropped to ~2.0; 20+20 quiet-host same-seed Tier 0 probe now gives Patricia 0.7484x SkipList but CV 7.45%, with SkipList CV 7.06%. Spotlight briefly peaked at 339% during a separate run. Root cause remains process scheduling/thread-creation jitter within the unchanged ~15ms timer window. Binding CV still fails; do not run the costly 5-seed matrix or enter persistence tasks until a valid stable window passes.
Task 5: full five-seed baseline/candidate-identical matrix rerun after the temporary quiet window. All 400 rows valid, zero errors, expected hash `454339457082336225`; single writer, four writers, and Arena gates pass all seeds. Eight-writer gates pass only seed 20260920; CV/ratio fail seed 20260921, CV fails seed 20260922, ratio/CV fail seed 20260923, and CV fails seed 20260924. Exact raw matrix is `/tmp/cedar-32x8-lowload-matrix.A3ajAo/matrix-raw/matrix.csv`; summary is `/tmp/cedar-32x8-lowload-matrix.A3ajAo/summary.json`. Gate remains failed; no source candidate is authorized by this result.
Task 5: evidence repair: replaced the ten-row placeholder `seed-isolated-8writer.csv` with all 100 raw rows from `/tmp/cedar-32x8-seed-isolation.2kUtSJ/raw/samples.csv`, added its header, and independently recomputed medians, ratios, and population CVs. The archived rows match byte-for-byte; ratios are 0.825894, 0.851285, 0.857151, 0.858681, and 0.817663 for seeds 20260920-20260924, and Radix CVs are 6.1756%, 7.3671%, 6.0836%, 6.7810%, and 7.1734%. Three ratio gates and all five CV gates still fail. Workload-distribution JSON parses and remains unchanged; no production or user-owned benchmark files were modified.
Task 3: complete (commits 520a5ce..c808202, tests: ctest --test-dir /Volumes/E/CedarBuild/pure-radix-cas-cursor-debug-20260921 -R ^PartitionedVersionRadixMemTableTest\. --output-on-failure → Total Test time (real) =   1.70 sec)
Task 4: complete (commits c808202..394db8e, tests: ctest --test-dir /Volumes/E/CedarBuild/pure-radix-cas-cursor-debug-20260921 -R ^Patricia32x8StabilityContract$ --output-on-failure → Total Test time (real) =   0.84 sec)
